User:
  - Add
  - getProducts
  - getSpecificOrder

Admin:
  - getOrders
  - getPurchases
  - sendCompletionEmail
  - complete
  - archive

Dev:
  - hash

New:
  - updateDB

Structure:
  get:
    - orderByKey (creator)
    - products (public)
    - orders (admin)
    - purchases (admin)
  set:
    - complete (admin)
    - archive (admin)
  db:
    - update (admin)
    - get (admin) [Same as getproducts?]
  email:
    - completionEmail (admin)
  - hash (admin)

Structure:
  orders:
    - add (public)
    - getByKey (creator)
    - get (admin)
    - complete (admin)
    - archive (admin)
  purchases:
    - get (admin)
  db:
    - update (admin)
    - get (admin)
    - hash (admin)
  email:
    - completionEmail (admin)
  
